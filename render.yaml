services:
  - type: web
    name: immigration-poster
    runtime: python
    buildCommand: pip install -r requirements.txt && python prerender_templates.py
    startCommand: gunicorn app:app --workers 1 --timeout 300
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
